[package]
name = "gauche"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
